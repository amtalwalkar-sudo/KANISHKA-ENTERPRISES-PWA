name: Phase 5 Maintenance persistence validation

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: phase-5-maintenance-persistence-${{ github.ref }}
  cancel-in-progress: true

jobs:
  maintenance-persistence:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Install dependencies
        run: npm install
      - name: Build application
        run: npm run build
      - name: Install Chromium
        run: timeout 180s npx playwright install --with-deps chromium
      - name: Validate Maintenance application boundary
        run: npm run validate:phase-5-maintenance-application
      - name: Validate Maintenance persistence round-trip (real IndexedDB)
        run: npm run validate:phase-5-maintenance-real-persistence

name: KFE legacy validation (manual only)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This legacy workflow is intentionally manual-only. The canonical automatic validation is KFE 2.0 single CI."

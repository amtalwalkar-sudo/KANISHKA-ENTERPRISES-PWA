name: Phase 5 Fuel persistence validation

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: phase-5-fuel-persistence
  cancel-in-progress: true

jobs:
  fuel-persistence:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node
        uses: actions/setup-node@v5
        with:
          node-version: 20
      - name: Install dependencies
        run: npm ci
      - name: Build application
        run: npm run build
      - name: Install Chromium
        run: timeout 180s npx playwright install --with-deps chromium
      - name: Validate Fuel application boundary
        run: npm run validate:phase-5-fuel-application
      - name: Validate Fuel persistence round-trip (real IndexedDB)
        run: npm run validate:phase-5-fuel-real-persistence

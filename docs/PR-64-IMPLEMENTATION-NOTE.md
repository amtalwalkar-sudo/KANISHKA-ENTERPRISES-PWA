PR #64 implements the frozen driver-facing Work UI interaction model.

The implementation keeps the existing Day/Shift/Trip store and repository operations intact. The changes are presentation-focused: contextual Day controls, top-center Shift toggle, fixed Trip action immediately above BottomNav, clearer state badges, driver-friendly copy, larger touch targets, and focused overlays/forms.
